/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["media.graphassets.com"],
    //remotePatterns: ["media.graphassets.com"],
  },
};

export default nextConfig;
